class Message {
    constructor(text, senderId, receiverId) {
        this.id;
        this.dateTime;
        this.text = text;
        this.senderId = senderId;
        this.receiverId = receiverId;
    }
}